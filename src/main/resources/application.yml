spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopdb
    username: shop
    password: shop123
  jpa:
    hibernate.ddl-auto: update   # 开发期自动建表
    show-sql: true
    properties:
      hibernate.format_sql: true
management.endpoints.web.exposure.include: health,info